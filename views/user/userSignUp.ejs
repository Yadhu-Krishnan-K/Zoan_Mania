<%-include('header');-%>

<section class="main-body">
    <div class="left-section d-flex justify-content-center align-items-center">
            <div class="login d-flex justify-content-center align-items-center">
                <div class="inner">
                    <div class="logo">
                        
                    </div>
                    <p class="text">Come clean us out!!!</p>
                    <h4>Sign up</h4>
                            <div id="alertDiv" class="alert"></div>

                    <form id="for-password"  action="/otpsend" method="post" >
                        <input type="text" class=" box" name="name" id="name" placeholder="Name" required><br>
                        <input type="email" class="mt-3 box" name="email" id="email" placeholder="Email" required><br>
                        <input type="password" class="mt-3 box" name="password" id="password" placeholder="Password" required><br>
                        <input type="password" class="mt-3 box" name="confirmPassword" id="confirmPassword" placeholder="Confirm Password" required><br>
                        <div style="width: 100%; display: flex; justify-content: center;"><input type="submit" class="mt-2 btn" value="Sign up" onclick="checkPass()" id="submit"></div>
                    </form>
                </div>

            </div>
    </div>
    <div class="right-section"></div>
    <a href="/auth/google">Login with google</a>
</section>

<script>
    function checkPass(){
        document.getElementById("for-password").addEventListener("submit", function (event) {
        var password = document.getElementById('password').value;
        var username = document.getElementById('name').value;
        username = username.trim()

        var confirmPassword = document.getElementById('confirmPassword').value;
        if(/\s/.test(username) && /\d/.test(username) && /[!@#$%^&*()_+{}\[\]:;<>,.?~\\\-]/.test(username)){
            event.preventDefault()
            alertDiv.textContent = "Check your user name";
        alertDiv.style.display = "block";

        // Hide the error message after 5 seconds
        setTimeout(function () {
            alertDiv.style.display = "none";
        }, 5000);
        }
        if(password===confirmPassword && password.length>=8 && /[a-zA-Z]/.test(password) && /\d/.test(password) && /[!@#$%^&*()_+{}\[\]:;<>,.?~\\\-]/.test(password) && !/\s/.test(password)){
            
        }else{
            event.preventDefault()
            alertDiv.textContent = "Password must contain at least 8 characters, including one alphabetic letter, one number, one special character, and no spaces. Passwords must also match.";
        alertDiv.style.display = "block";

        // Hide the error message after 5 seconds
        setTimeout(function () {
            alertDiv.style.display = "none";
        }, 5000);
        }
    })
    }

</script>

<%-include('footer');-%>